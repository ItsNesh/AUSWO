var express = require('express');
var router = express.Router();

// Dashboard routes can be added here if needed
// For now, this is a placeholder since the frontend uses direct API calls

module.exports = router;
